import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import { html } from 'lit-html';

<Meta title="My Component" component="my-component" />

# My Component

With `MDX` we can define a story for `MyComponent` right in the middle of our markdown documentation.

<Preview>
  <Story name="all checkboxes">
    {html`
      Stencil + Storybook + MDX
      <hr>
      <my-component first="Stencil" last="'Don't call me a framework' JS"></my-component>
    `}
  </Story>
</Preview>
